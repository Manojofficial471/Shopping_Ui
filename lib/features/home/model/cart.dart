class Cart {
  String image;
  int quantity;
  String title;
  String description;

  Cart({
    required this.description,
    required this.title,
    required this.image,
    required this.quantity,
  });
}
